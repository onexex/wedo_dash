<?php
/* Forwarder — the real DB config lives in the project-root w_conn.php.
   Kept here only so existing `include 'w_conn.php'` calls from this directory
   keep working. Do not add credentials here. */
require __DIR__ . '/../w_conn.php';
